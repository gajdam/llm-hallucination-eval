# Lightweight deps for CI — no torch/transformers (not needed for LLM connection tests)
anthropic>=0.40.0
openai>=1.0.0
requests>=2.31.0
python-dotenv>=1.0.0
pydantic>=2.5.0
PyYAML>=6.0
pytest>=8.0.0
